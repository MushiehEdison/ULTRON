# STT
faster-whisper
sounddevice
numpy

# Intent parsing
anthropic
python-dotenv

# Automation
pyautogui
pywinauto; sys_platform == 'win32'
pyobjc; sys_platform == 'darwin'
python-xlib; sys_platform == 'linux'

# GUI
PyQt6
qtawesome

# Packaging
pyinstaller

# Testing
pytest
